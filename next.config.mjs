/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['b.wallet.ir'], // اضافه کردن دامنه‌ای که تصاویر از آن بارگذاری می‌شود
    },
  };
  
  export default nextConfig;
  